pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://dl.bintray.com/kotlin/kotlin-eap/' }
    }
}

rootProject.name = 'kotlin-language-server'

include(
    'shared',
    'server',
    'grammars'
)
