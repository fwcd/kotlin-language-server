matrix:
  include:
    - language: java
      install: ./gradlew install
      script: ./gradlew build -i
    
    - language: node_js
      install:
        - npm install
        - npm run compile
      script: npm run test --silent
